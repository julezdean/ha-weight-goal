name: hassfest

on:
  push:
  pull_request:
  schedule:
    - cron: "23 4 * * *"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master
